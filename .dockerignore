# Dependencies
node_modules
**/node_modules

# Build outputs
dist
**/dist
*.tsbuildinfo

# Secrets — never into an image
.env
.env.local
.env.*.local

# Local uploads (dev attachment storage)
.local-uploads

# VCS / editor noise
.git
.gitignore
.DS_Store

# Generated Docker-edition bundle (rebuilt inside the image)
editions/docker/server.js
editions/docker/pino-*.js
editions/docker/thread-stream-worker.js
editions/docker/dist
