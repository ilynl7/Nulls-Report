# Secrets & local env — never commit these
.env
.env.local
.env.*.local

# dependencies
node_modules

# local attachment storage
.local-uploads/

# logs & misc
npm-debug.log
yarn-error.log
.DS_Store
Thumbs.db

# NOTE: `server.js` and `dist/` are the product on this branch and MUST stay
# tracked. Do not add ignore rules for them here.
